services:
  backend:
    image: edu-hub-backend:latest
    container_name: edu-hub-backend
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    env_file:
      - .env
    ports:
      - "5000:5000"
