version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
      container_name: edu-hub-backend-container
    working_dir: /app
    env_file:
      - .env
    ports:
      - "5000:5000"
